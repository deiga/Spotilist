%article.object.user
  %h1>
    = @user.name 
    %small>
      (
      = @user.display_name 
      )

  - unless @starred_tracks.empty?
    .clear
    %section.starred.tracks
      %h2>Starred Tracks
      %p>
        = @starred_tracks.map { |track| link_to track.name, track }.join(', ')
